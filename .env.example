# Resend API key used by the server (server.ts) to send emails from the contact form.
# Get one at https://resend.com — never commit the real value or expose it in client code.
RESEND_API_KEY=

# Email address that receives messages sent through the contact form.
CONTACT_TO_EMAIL=julian_2120@hotmail.com
